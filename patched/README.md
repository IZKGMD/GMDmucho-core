# Patched clients

This directory contains patched client builds produced for MuchoCore.

- `apk/` — patched Android APK files.
- `exe/` — patched Windows EXE files.

Keep only distributable patched clients here. Signing keys, credentials, temporary build files, and other secrets must never be committed.
